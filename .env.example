# =========================================================
# NUTRISCAN AI — ENVIRONMENT CONFIGURATION TEMPLATE
# =========================================================

# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=nutriscan
DB_PORT=3306

# Flask Secret Key
FLASK_SECRET_KEY=nutriscan_secret_key_2026

# AI Provider Settings (HuggingFace / Optional IBM watsonx / Gemini)
# Leave blank for default local CLIP vision model
HF_TOKEN=
IBM_WATSONX_API_KEY=
GEMINI_API_KEY=
